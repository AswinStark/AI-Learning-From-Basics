# Variable is a way for us to store data. Basically they are just data containers.
variable_int = 10
variable_string = "Aswin"
variable_list = ["Harry", "Potter", "Hermoine"]

print(f"integer stored = {variable_int}, string stored = {variable_string}, list stored = {variable_list}")